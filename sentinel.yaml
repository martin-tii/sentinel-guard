allowed_paths:
  - "./workspace"
blocked_paths:
  - "/etc"
  - "~/.ssh"

allowed_commands:
  - "echo"
  - "ls"

allowed_hosts:
  - "openai.com" # Will match api.openai.com due to .endswith logic
  - "pypi.org"

blocked_keywords:
  - "ignore previous instructions"